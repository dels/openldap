# openldap
OpenLDAP bitnami clone with all variables extraced to .env.
Automatically binds to IPv4 and IPv6. 
Also upgrades to debian trixie while build phase.
Automatically generates ssl certificates during build phase.

## AI created
All enhancements are done using AI (gemini).  

## first steps
cp .env.example .env

customize .env